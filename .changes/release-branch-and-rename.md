---
type: major
---

Rename CLI command from `cc` to `champ`.

- Binary renamed from `bin/cc` to `bin/champ`
- Add configurable release branch prefix via `releaseBranchPrefix` config option
- Changeset workflows now run after Tests and Lint workflows complete
