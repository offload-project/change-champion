---
type: minor
---

Add automatic issue linking in changelogs.

- Issue references like `#123` are automatically linked to the repository
- Auto-detects repository URL from git remote or uses `repository` config option
