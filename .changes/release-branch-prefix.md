---
type: minor
---

Add configurable release branch prefix and run changeset workflows after lint/tests.

- Custom release branch prefix via `releaseBranchPrefix` config option (default: `changeset-release/`)
- Changeset workflows now run after Tests and Lint workflows complete
