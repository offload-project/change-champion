---
type: minor
---

Add `cc preview` command to show CHANGELOG output before running version.

- Shows current and next version
- Displays the generated changelog entry
- Supports `--prerelease` flag to preview pre-release versions
